# Copy this file to `docker.env` and override values as needed before running `docker compose up`.

MONGO_URI=mongodb://mongo:27017/lifetrack
JWT_SECRET=change-me
JWT_EXPIRES_IN=7d
HF_API_KEY=
HF_MODEL=facebook/bart-large-mnli
VITE_API_URL=http://backend:5000/api
